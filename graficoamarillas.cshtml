@{
                  
                  var connexion = Database.Open("jugadores");
                  var peticion3 = connexion.Query("SELECT TOP 5 nombre,amarillas FROM plantilla ORDER BY amarillas DESC");
              
                  var myChart = new Chart(width:200, height:200)
                    
                        .AddTitle ("Amarillas")
                        .AddSeries(chartType:"Column",
                        xValue:peticion3, xField :"nombre",
                        yValues:peticion3, yFields :"amarillas"

                        )

                        .Write();
                       
}
