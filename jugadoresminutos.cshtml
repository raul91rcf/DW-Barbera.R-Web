@{
    var titulo="PÁGINA";

    var connexion = Database.Open("jugadores");
    
    var peticion = connexion.Query("SELECT nombre,minutos FROM plantilla ORDER BY minutos ASC ");
    var peticion2 = connexion.Query("SELECT nombre,minutos FROM plantilla ORDER BY minutos DESC ");
}
<!DOCTYPE html>

<html lang="es">
    <head>
<meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
        <meta charset="utf-8" />
        <title>@titulo</title>
        <link href="~/favicon.ico" rel="shortcut icon" type="image/x-icon" />
        <link rel="stylesheet" href="estilos.css">
    </head>
    <body>
         <div id="contenedor">
              @RenderPage("includes/Cabecera.cshtml")
            <section>
                <div id="divcriterio9">
                     <table id="tablaglobal">
                        <tr>
                            <td id="tdglobaltitulo">Nombre</td>
                            <td id="tdglobaltitulo">Minutos (ASC)</td>
                        </tr>
                    @foreach (var fila in peticion)
                    {
                        <tr>
                        <td id="tdglobalnormal"><h3>@fila.nombre</h3></td>
                        <td id="tdglobalnormal"><h3>@fila.minutos</h3></td>
                        </tr>
                    }
                    </table>
                 </div>

                <div id="divcriterio10">
                     <table id="tablaglobal">
                        <tr>
                            <td id="tdglobaltitulo">Nombre</td>
                            <td id="tdglobaltitulo">Minutos (DESC)</td>
                        </tr>
                    @foreach (var fila in peticion2)
                    {
                        <tr>
                        <td id="tdglobalnormal"><h3>@fila.nombre</h3></td>
                        <td id="tdglobalnormal"><h3>@fila.minutos</h3></td>
                        </tr>
                    }
                    </table>
                 </div>

                <div id="graficominutos">
                    <img src="graficominutos" alt="graficominutos"></img>
                </div>

            </section>
              
        </div>
    </body>
</html>
