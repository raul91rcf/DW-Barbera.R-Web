@{
                  
                  var connexion = Database.Open("jugadores");
                  var peticion3 = connexion.Query("SELECT TOP 5 nombre,asistencias FROM plantilla ORDER BY asistencias DESC");
              
                  var myChart = new Chart(width:200, height:200)
                    
                        .AddTitle ("Asistencias")
                        .AddSeries(chartType:"Column",
                        xValue:peticion3, xField :"nombre",
                        yValues:peticion3, yFields :"asistencias"

                        )

                        .Write();
                       
}
