@{
                  
                  var connexion = Database.Open("jugadores");
                  var peticion3 = connexion.Query("SELECT TOP 5 nombre,titular FROM plantilla ORDER BY titular DESC");
              
                  var myChart = new Chart(width:200, height:200)
                    
                        .AddTitle ("Titular")
                        .AddSeries(chartType:"Column",
                        xValue:peticion3, xField :"nombre",
                        yValues:peticion3, yFields :"titular"

                        )

                        .Write();
                       
}
